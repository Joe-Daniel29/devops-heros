# Single-stage equivalent, kept only to compare final image size
# against the multi-stage build in ./Dockerfile
FROM node:22-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 8080
CMD ["npm", "start"]
